{
	"name": "pi-trace-workspace",
	"private": true,
	"scripts": {
		"build": "pnpm --filter pi-tracing build",
		"test": "pnpm --filter pi-tracing test",
		"check": "biome check .",
		"changelog": "git-cliff -o CHANGELOG.md",
		"changelog:unreleased": "git-cliff --unreleased -o CHANGELOG.md"
	}
}
